@* @model diveWebMVC.Models.TUproduct *@
@model diveWebMVC.ViewModels.TUproductsviewmodels
@{
    ViewData["Title"] = "Create";
}
@section Styles {
    <style>
        .container {
            width: 100%;
            max-width: none;
            padding: 40px; /* 增加內邊距 */
            margin: 0 40px; /* 增加左右外邊距 */
        }

    </style>
}
<div class="container">
    <div>
        <h1>新增商品</h1>
    </div>
    <hr />
    <div >
        <div class="row">
            <div class="col-md-4">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="SellerId" class="control-label"></label>
                        <select asp-for="SellerId" class="form-control" asp-items="ViewBag.SellerId"></select>
                    </div>
                    <div class="form-group">
                        <label asp-for="CategoryId" class="control-label"></label>
                        <select asp-for="CategoryId" class="form-control" asp-items="ViewBag.CategoryId"></select>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProductName" class="control-label"></label>
                        <input asp-for="ProductName" class="form-control" />
                        <span asp-validation-for="ProductName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProductDescription" class="control-label"></label>
                        <input asp-for="ProductDescription" class="form-control" />
                        <span asp-validation-for="ProductDescription" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProductPrice" class="control-label"></label>
                        <input asp-for="ProductPrice" class="form-control" />
                        <span asp-validation-for="ProductPrice" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="UpdatedAt" class="control-label"></label>
                        <input asp-for="UpdatedAt" class="form-control" />
                        <span asp-validation-for="UpdatedAt" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="CreatedAt" class="control-label"></label>
                        <input asp-for="CreatedAt" class="form-control" />
                        <span asp-validation-for="CreatedAt" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProductConditionId" class="control-label"></label>
                        <select asp-for="ProductConditionId" class="form-control" asp-items="ViewBag.ProductConditionId"></select>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProductStatus" class="control-label"></label>
                        <input asp-for="ProductStatus" class="form-control" />
                        <span asp-validation-for="ProductStatus" class="text-danger"></span>
                    </div>
                    <!-- 上傳多張圖片 -->
                    <div class="form-group">
                        <label>上傳圖片</label>
                        <input type="file" name="Images" accept="image/*" multiple class="form-control" />
                        <span asp-validation-for="Image" class="text-danger"></span>
                    </div>

                    @* <!-- 顯示上傳的圖片並選擇封面 -->
                    <div class="form-group">
                        <label>選擇封面圖片</label>
                        <div id="image-preview-container">
                            <!-- 圖片預覽 -->
                            @if (Model.TUproductImages != null && Model.TUproductImages.Count() > 0)
                            {
                                foreach (var image in Model.TUproductImages)
                                {
                                    <div class="form-check">
                                        <img src="@($"data:image/jpeg;base64,{Convert.ToBase64String(image.Image)}")" class="image-preview" />
                                        <input type="radio" name="CoverImageId" value="@image.ProductImages" class="form-check-input" />
                                        <label class="form-check-label">設為封面</label>
                                    </div>
                                }
                            }
                        </div>
                    </div> *@
                    <div class="form-group">
                        <label>上傳圖片</label>
                        <input type="file" name="Images" accept="image/*" multiple class="form-control" />
                        <span asp-validation-for="Image" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label>選擇封面圖片</label>
                        <div id="image-preview-container">
                            <!-- 檢查 Model 是否為空 -->
                            @if (Model == null)
                            {
                                <p>模型為空，無法加載數據。</p>
                            }
                            else
                            {
                                <!-- 如果 Model 非空，檢查 TUproductImages 是否有數據 -->
                                @if (Model.TUproductImages != null && Model.TUproductImages.Any())
                                {
                                    foreach (var image in Model.TUproductImages)
                                    {
                                        <div class="form-check">
                                            @if (image.Image != null)
                                            {
                                                <img src="@($"data:image/jpeg;base64,{Convert.ToBase64String(image.Image)}")" class="image-preview" />
                                            }
                                            <input type="radio" name="CoverImageId" value="@image.ProductImages" class="form-check-input" />
                                            <label class="form-check-label">設為封面</label>
                                        </div>
                                    }
                                }
                                else
                                {
                                    <p>沒有圖片可用。</p>
                                }
                            }
                        </div>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
        <br />
        <div>
            <a asp-action="Index">返回上頁</a>
        </div>
    </div>
</div>



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
