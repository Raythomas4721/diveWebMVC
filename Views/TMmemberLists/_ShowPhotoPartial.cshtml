@model TMmemberList
@inject diveShopperContext _context
@{
	TMmemberList? member = await _context.TMmemberLists.FindAsync(Model.MemberId);
	if(member != null && member.MemberPhoto!=null) {
		<img style="width:320px;height:240px" src='@Url.Action("GetPicture","TMmemberList",new {id = Model.MemberId})' title="@Model.MemberName" />
	}
	else {
		<img style="width:320px;height:240px" src='@Url.Content("~/images/NoPerson.png")' title="尚無圖片" />
	}
}